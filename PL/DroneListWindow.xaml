<Window x:Class="PL.DroneListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="DroneListWindow" Height="450" Width="800">
    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="400*"/>
        </Grid.RowDefinitions>
        <Grid Name="UpGrid" HorizontalAlignment="Stretch" Height="auto" Grid.Row="0"
        VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions >
            <ComboBox Name="ComboStatusSelector" 
            HorizontalAlignment= "Stretch"
            VerticalAlignment="Stretch"
            Grid.Column="1"
            ToolTip="choose a status" SelectionChanged="filter"/>
            <Button x:Name="addDrone"  Content="Add Drone" Grid.Column="2" HorizontalAlignment= "Stretch" VerticalAlignment="Stretch" FontSize="15" Click="addDrone_Click"/>
            <Button x:Name="CloseWindowDroneList" Content="Close Drone List Window" Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="15" Click="CloseWindowList_Click"/>
            <ComboBox x:Name="weigjtSelector" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="48" SelectionChanged="select"/>
        </Grid>
        <TextBox x:Name="Dronestatus" HorizontalAlignment="Left" Margin="241,0,0,0" Text="Drone Status:" TextWrapping="Wrap" VerticalAlignment="Center" Width="120"/>
        <TextBox x:Name="DroneWeight" HorizontalAlignment="Left" Margin="40,0,0,0" Text="Drone Weight:" TextWrapping="Wrap" VerticalAlignment="Center" Width="120"/>
        <ListView x:Name="DroneListView" Grid.Row="1"  Margin="0,0,0,20" MouseDoubleClick="DoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>


</Window>