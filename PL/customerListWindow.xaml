<Window x:Class="PL.customerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="customerListWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="myLableStyle"
TargetType="Label">
            <Setter Property="FontSize"
 Value="12" />
            <Setter Property="FontFamily"
 Value="Consolas" />
            <Setter Property="FontWeight"
 Value="Bold" />
        </Style>

        <Style x:Key="myButtonStyle"
 TargetType="Button">
            
            <Setter Property="FontSize"
 Value="12" />
            <Setter Property="FontFamily"
 Value="Consolas" />
            <Setter Property="FontWeight"
 Value="Bold" />
        </Style>
    </Window.Resources>
    <Grid>
       
        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="11*" />
            <RowDefinition Height="40*" />
        </Grid.RowDefinitions>
        <Button x:Name="btnAddCustomer" Content="add customer" Grid.Column="0" Grid.Row="0"  HorizontalAlignment="Left" Style = "{StaticResource myButtonStyle}" Click="btnAddCustomer_Click"/>
        <ListBox x:Name="customerListBox" ItemsSource="{Binding}" MouseDoubleClick="DoubleClick"  Grid.Column="0" Grid.Row="1" FontSize="22" />

    </Grid>
</Window>
